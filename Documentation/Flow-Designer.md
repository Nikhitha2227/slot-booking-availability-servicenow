# Flow Designer

Flow Designer is used to automate notifications related to booking status.

## Booking Status Notification

A flow is configured to send a notification when the booking status changes.

The notification keeps the requester informed about the current status of the booking.

The flow helps automate communication instead of requiring the status update to be communicated manually.
